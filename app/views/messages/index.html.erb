<%= turbo_frame_tag "chat_room_#{chat_room.id}_messages" do %>
  <% if @messages.empty? %>
    <div class="mt-3">
      <p class="text-gray-600 text-center">No messages were sent yet.</p>
    </div>
  <% else %>
    <%= render @messages %>
  <% end %>
<% end %>
